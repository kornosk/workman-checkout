<section class="section">
  <div class="container box">
    <div class="heading columns">
      <div class="column is-8 is-offset-2">
        <h3 class="title is-3">New Working Date</h3>
      </div>
    </div>
    <hr class="margin-top-0">
    <div class="body columns">
      <div class="column is-8 is-offset-2">
        <%= form_for ([@user, @working_date]), html: { class: "control" } do |f| %>
          <!-- Check Errors -->
          <% if @working_date.errors.any? %>
            <div id="error_explanation">
              <h2>
                <%= pluralize(@working_date.errors.count, "error") %> prohibited
                this working_date from being saved:
              </h2>
              <ul>
                <% @working_date.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>


          <!-- Datetime Picker -->
          <div class="columns control is-horizontal">
            <div class="control-label column is-1 flex-item-center--vertical">
              <%= f.label :date, :placeholder => "Date" %><br>
            </div>
            <div class="control column">
              <%= date_select :date, :date, {}, { :class => 'input-group form-control' } %>
            </div>
          </div>
          <div class="columns control is-horizontal">
            <div class="control-label column is-1 flex-item-center--vertical">
              <%= f.label :start, :placeholder => "Start" %>
            </div>
            <div class="control column">
              <%= f.time_select :start, {}, { :class => 'input-group form-control' } %>
            </div>
          </div>
          <div class="columns control is-horizontal">
            <div class="control-label column is-1 flex-item-center--vertical">
              <%= f.label :end, :placeholder => "End" %>
            </div>
            <div class="control column">
              <%= f.time_select :end, {}, { :class => 'input-group form-control' } %>
            </div>
          </div>
          <div class="columns control is-horizontal">
            <div class="column">
              <%= f.submit 'Submit', class: "button is-primary margin-right-5px float-right" %>
              <%= link_to "Cancel", user_path(@user), class: "button btn-back margin-right-5px float-right"%>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>